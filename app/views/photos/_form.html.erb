<%= simple_form_for [@event, photo], html: {multipart: true } do |f| %>
  <% render partial: 'partials/form_errors', locals: {model: photo} %>

  	<%= f.input :photo, as: :file %>
  	<%= f.submit t('buttons.upload'), class: 'btn btn-primary' %>
<% end %>